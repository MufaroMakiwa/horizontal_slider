class Node {
  /*
  Each node has a data attribute that represents the card element and a pointer that points to 
  the next element in the array
  */

}


export class InfiniteArray {
  
  static insert = (object) => {
    /*
    Given an object, create a node and insert it at the end of the infnite array
    */
  }

  static next = () => {
    /*
    Move the head of the array to the next element and make the previous head the tail
    */
  }
}

